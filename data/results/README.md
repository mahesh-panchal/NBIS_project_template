# Results

Outputs published by workflow/notebook runs launched from
[`analyses/`](../../analyses/README.md), typically one subfolder per
analysis (matching the numbered folder it was produced from).

*Contents here are generated, not authored: they can always be
reproduced by re-running the corresponding analysis, so this folder is
usually not committed to git (see `.gitignore`).*
